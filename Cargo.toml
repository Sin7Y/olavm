[workspace]
members = [
    "assembly",
    "vm-core",
    "vm-circuits",
    "processor",
    "prover",
    "verifier",
    "olavm",
    "plonky2",
    "plonky2-evm",
    "field",
]

[profile.release]
codegen-units = 1
lto = true

[profile.bench]
codegen-units = 1
lto = true